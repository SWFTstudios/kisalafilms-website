{
  "name": "kisalafilms-website",
  "private": true,
  "version": "0.1.0",
  "description": "Kisala Films marketing site — motorcycle wraps × cinema",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "preview": "wrangler versions upload",
    "build": "bash scripts/build.sh",
    "test": "node scripts/test-pages.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.110.0"
  }
}
