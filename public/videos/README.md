Drop a muted looping MP4 here as `hero-loop.mp4` for:

- the homepage video hero
- the Film Video split column (falls back to a Vimeo background loop until the file is present)

Keep files under Cloudflare’s ~25 MiB Worker asset limit. Until then posters / Vimeo are used.
